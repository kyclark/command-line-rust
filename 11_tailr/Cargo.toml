[package]
name = "tailr"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
clap = { version = "4.1.4", features = ["derive"] }
num = "0.4"
regex = "1"
once_cell = "1"

[dev-dependencies]
assert_cmd = "2"
predicates = "2"
rand = "0.8"
